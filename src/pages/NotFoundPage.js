import React from "react";
import NotFound from "../components/ErrorDisplay/NotFound";

const NotFoundPage = () => {
    return (
        <section>
            <NotFound />
        </section>
    );
};

export default NotFoundPage;
